import 'package:flutter/material.dart';

class ConvertPage extends StatelessWidget {
  const ConvertPage({super.key});

  @override
  Widget build(BuildContext context) {
    // Replace with the actual content for the Convert page
    return const Center(
      child: Text('Convert Page Content', style: TextStyle(fontSize: 24)),
    );
  }
}
